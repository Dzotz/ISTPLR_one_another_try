@model Query

@{
    ViewData["Title"] = "SimpleQuery3";
}

<h1>Запит 3</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                Назва
            </th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.CatNames.Count; i++)
        {
            <tr>
                <td>
                    @Model.CatNames[i]
                </td>
            </tr>
        }
    </tbody>
</table>